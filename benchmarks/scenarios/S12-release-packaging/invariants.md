# Expected invariants

The release artifact excludes credentials, local environment files, source-control metadata, caches, and development-only functionality. Production debug behavior is disabled. Keep only files required to run or diagnose the intended production build. ASVS anchors: `v5.0.0-13.4.1`, `v5.0.0-13.4.2`, `v5.0.0-15.2.3`.
