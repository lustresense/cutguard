# Expected invariants

Risk: low. No new trust boundary. Reuse the existing spacing scale. Change only the intended component or token reference. Do not add validation, middleware, security dependencies, or architecture.
