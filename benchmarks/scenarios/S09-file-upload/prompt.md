# Task

Add authenticated profile-image upload using the project's existing object-storage client and image-processing path. Accept JPEG, PNG, and WebP files up to 5 MB, generate the storage name on the server, and associate the object with the current user. Do not trust the original filename or browser-provided MIME type, and do not add a new upload framework unless the existing stack cannot satisfy a stated requirement.
