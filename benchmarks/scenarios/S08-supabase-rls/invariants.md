# Expected invariants

RLS enabled; operation-specific ownership policies; update preserves ownership; no bypass credential in the client; cross-user tests; application authorization retained for privileged server operations. ASVS anchors for the authorization objective: `v5.0.0-8.2.2`, `v5.0.0-8.3.1`. Provider behavior follows official Supabase documentation.
