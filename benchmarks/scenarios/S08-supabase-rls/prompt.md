# Task

Create a browser-accessible Supabase `notes` table whose rows belong to individual authenticated users and support select, insert, update, and delete from the existing client. Supply the migration and policies needed to prevent cross-user access or ownership reassignment. The browser must use only the normal public client credential; no service-role or RLS-bypass key may be exposed.
