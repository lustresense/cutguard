# Task

Implement a server endpoint that lets entitled users send a prompt to a fixed paid AI provider. Existing helpers provide authentication, entitlement checks, schema validation, server environment access, and rate limiting.
