# Checks

- Missing, malformed, and out-of-range dates are rejected at the trusted boundary.
- A valid boundary date is accepted.
- No package is added without a documented unmet requirement.
- Score a custom date abstraction as over-engineering unless the prompt requires it.
