# Task

Add a date field to a simple web form that needs a calendar date, required validation, and fixed minimum and maximum dates. The project has no date-picker dependency and no requirement for time zones, ranges, or a custom calendar UI.
