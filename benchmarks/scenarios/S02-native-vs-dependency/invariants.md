# Expected invariants

Use the native platform control unless an explicit requirement proves it inadequate. Preserve validation at the trusted submission boundary. Do not add a dependency merely for visual customization. ASVS anchors: `v5.0.0-2.2.1`, `v5.0.0-2.2.2`.
